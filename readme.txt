Copy this folder to any location

install node

after successful node installation, from the above folder location run command

npm install

once all the dependencies are installed, run command

node app.js > /dev/null 2>&1 &